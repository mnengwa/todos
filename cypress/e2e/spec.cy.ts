describe("Todo app", () => {
    it("opens successfully", () => {
        cy.visit("http://localhost:4200");
    });

    it("can create a new task", () => {
        const navbarInput = cy.get("nav");

    });
});
